# PyTo-Do Dependencies
# Core Python libraries (built-in):
# - tkinter (for GUI)
# - json (for data storage)
# - os, sys (for system operations)
# - datetime (for timestamps)
# - argparse (for CLI arguments)
# - pathlib (for file paths)
# - signal (for interrupt handling)

# Optional cloud sync dependencies (install as needed):
# google-auth-oauthlib>=0.5.0
# google-api-python-client>=2.0.0
# dropbox>=11.0.0

# Build dependencies (for creating executables):
# pyinstaller>=5.0.0

